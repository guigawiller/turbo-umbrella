apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "willer-helm.fullname" . }}-test-connection"
  labels:
    {{- include "willer-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "willer-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
